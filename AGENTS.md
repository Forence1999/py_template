# Python Project Template

通用 Python 项目模板，提供现代化工具链配置和标准项目结构。

## 项目约定
- **工具配置**：统一放入 `pyproject.toml`，不单独创建 `uv.toml`、`ruff.toml` 等配置文件
- **包管理器**：使用 uv 管理依赖，不使用 pip
- **Python 运行**：使用 `uv run` 执行脚本，不调用系统 python
- **环境变量**：使用 `.env` 文件配置，不硬编码在代码中
- **方案设计**：遵循业界最佳实践、第一性原理、当前项目现状，给出最优雅的方案
- **工具缺失处理**：如果执行任务时发现缺少命令行工具，先简要说明该工具的作用，再询问是否需要安装，不直接安装
- **不后向兼容**：重构或升级时默认不保留旧接口，保持代码整洁清爽
- **避免 Plan & Explore 自动模式**：没有明确指示时，不要自行调用 plan & explore 模式

## 文档规范
- **骨架优先**：模板型文档先锁定骨架；新增要求优先回写原段、原节、原结构，只有现有结构无法承载时才新增小节。
- **大改重写**：连续多轮修改同一文档时，若新要求已影响前提、结构或口径，直接重写受影响章节，不做碎片化补丁。
- **交付统稿**：交付前必须统稿：统一标题、术语、口径、顺序、示例和版本信息，删除重复、过渡句、兼容式表述和讨论痕迹；最终稿要像一次写成，有成文感和主线感。
